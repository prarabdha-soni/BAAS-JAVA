spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/inferencedb_dev
    username: inference_dev
    password: devpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect 