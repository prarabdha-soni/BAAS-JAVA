spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/projectdb_dev
    username: project_dev
    password: devpassword
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect 